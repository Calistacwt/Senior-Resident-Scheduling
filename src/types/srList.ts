export type srList = {
  id: string;
  name: string;
  mobile: string;
  email: string;
  MCR: string;
  noSession: string;
  remarks: string;
  postingPeriod: {
    startDate: string;
    endDate: string;
  };
  callDates: string[];
  leaves: string[];
};
